<!DOCTYPE html>
<html lang="en">
  <head>
      <title>{{#if title}}{{title}}{{else}}FAMHE{{/if}}</title>
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <!-- App Style -->
      <link rel="stylesheet" href="/lib/bootstrap/css/bootstrap.min.css" type="text/css" />
      <link rel="stylesheet" href="/css/stylesheet.css" type="text/css" />
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  </head>
  <body>
    <img id="bg-pattern" src="/images/bg.svg" alt="Background Image" />
    <div class="container">
      <header>
        <nav class="navbar navbar-expand-md "> {{!-- bg-dark navbar-dark --}}
          <div class="container-fluid">
            <a class="navbar-brand" href="/">{{#if brand}}{{brand}}{{else}}FAMHE{{/if}}</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                  <a class="nav-link" data-bs-toggle="offcanvas" href="#categories" role="button" aria-controls="categories">Categories</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" data-bs-toggle="offcanvas" href="#platforms" role="button" aria-controls="platforms">Platforms</a>
                </li>
                <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" id="navTools" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Tools
                  </a>
                  <ul class="dropdown-menu" aria-labelledby="navTools">
                    <li><a class="dropdown-item" href="/vendors">Vendors List</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="/categories">Categories List</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="/platforms">Platforms List</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="/applications">Applications List</a></li>
                    <li><a class="dropdown-item" href="/applications/new">Register a new application</a></li>
                  </ul>
          </li>
              </ul>
            </div>
            <div class="navRightBar">
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                  <a class="nav-link" href="/" role="button" data-bs-toggle="modal" data-bs-target="#accModal">Accounts</a>
                </li>
              </ul>
            </div>
          </div>
        </nav>
      </header>
      <main>
        {{{body}}}
        <!-- Categories offcanvas -->
        <div class="offcanvas offcanvas-start" tabindex="-1" id="categories" aria-labelledby="categoriesLabel">
          <div class="offcanvas-header">
            <h5 class="offcanvas-title" id="categoriesLabel">Categories</h5>
            <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas" aria-label="Close"></button>
          </div>
          <div class="offcanvas-body">
            <p class="sm-letter">
              Here are the categories used to classify the applications, selected from the list of categories below to filter all application based on the selected category. 
            </p>
            <ul class="list-group">
              {{#each categories}}
                <a href="/category/{{categoryID}}" class="list-group-item list-group-item-action" aria-current="true">{{name}}</a>
              {{/each}}
            </ul>
          </div>
        </div>
        <!-- Platforms offcanvas -->
        <div class="offcanvas offcanvas-start" tabindex="-1" id="platforms" aria-labelledby="platformsLabel">
          <div class="offcanvas-header">
            <h5 class="offcanvas-title" id="platformsLabel">Platforms</h5>
            <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas" aria-label="Close"></button>
          </div>
          <div class="offcanvas-body">
            <p class="sm-letter">
              Here are the platforms targeted by the registered applications.Selected from the list below to filter all application based on the desired target platform. 
            </p>
            <ul class="list-group">
              {{#each platforms}}
                <a href="/platform/{{platformID}}" class="list-group-item list-group-item-action" aria-current="true">{{name}}</a>
              {{/each}}
            </ul>
          </div>
        </div>
        <!-- Modal -->
        <div class="modal fade" id="accModal" tabindex="-1" aria-labelledby="accModalLabel" aria-hidden="true">
          <div class="modal-dialog">
            <div class="modal-content">
              <div class="modal-header">
                <h5 class="modal-title" id="accModalLabel">Accounts</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
              </div>
              <div class="modal-body">
                <div class="input-group mb-3">
                  <span class="input-group-text" id="basic-addon1">Username</span>
                  <input type="text" class="form-control" placeholder="Username" aria-label="Username" aria-describedby="basic-addon1">
                </div>
                <div class="input-group mb-3">
                  <span class="input-group-text" id="basic-addon2">Password</span>
                  <input type="password" class="form-control" placeholder="Password" aria-label="Password" aria-describedby="basic-addon2">
                </div>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button type="button" class="btn btn-info">Sign up</button>
                <button type="button" class="btn btn-primary">Sign in</button>
              </div>
            </div>
          </div>
        </div>
        <!--  -->
      </main>
      <footer>
        <img id="bg-pattern2" src="/images/bg.svg" alt="" />
        <div class="mt-5 dropdown-divider"></div>
        {{{section "footer-section"}}}
        <div class="container-fluid mt-4">
          <div class="row ">
            <div class="col-lg">
              <h3>FAMHE</h3>
              <p class="my-3">Our slogan that will impress the user</p>
              <div>
                
                <a data-bs-toggle="offcanvas" href="#categories" role="button" aria-controls="categories"><button type="button" class="btn btn-primary our-color">Categories</button></a>
                <a href="#"><button type="button" class="btn btn-outline-secondary">Back to top!</button></a>
              </div>
            </div>
          </div>
          <div class="row mt-4">
            <div class="col-lg">
              <p class="mb-1 sm-letter">Fernando, Asfari, Enrique</p>
            </div>
            <div class="d-flex p-3 mb-3 col-lg align-items-end justify-content-end">
              <a data-bs-toggle="offcanvas" href="#categories" class="px-4 sm-letter">Categories</a>
              <a data-bs-toggle="offcanvas" href="#platforms" class="px-4 sm-letter">Platforms</a>
              <a href="#" data-bs-toggle="modal" data-bs-target="#accModal" class="px-4 sm-letter">Accounts</a>
            </div>
          </div>
        </div>
      </footer>
    </div>
  </body>
  <script src="/lib/@popperjs/core/umd/popper.min.js" type="text/javascript"></script>
  <script src="/lib/bootstrap/js/bootstrap.min.js" type="text/javascript"></script>
  {{{section "scripts-section"}}}
</html>
